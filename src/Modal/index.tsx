import Modal, { ModalProps } from './Modal'
import useModal from './useModal'
import type { CloseFunction, ModalInstance, UseModalReturnValue } from './types'

export type { ModalProps, CloseFunction, ModalInstance, UseModalReturnValue }

export { useModal }

export default Modal
